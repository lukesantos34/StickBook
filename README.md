# StickBook

Mobile-first stickball scorekeeper (React + TypeScript + Vite)

Run:

```bash
npm install
npm run dev
```

Features: new game setup, live scoring with manual base state, undo, end game, report and share via URL.
